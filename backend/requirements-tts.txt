# TTS dependencies for Aoi voice synthesis (Style-BERT-VITS2)
# Install with: pip install -r backend/requirements-tts.txt
#
# Also requires PyTorch (CPU):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# After installing, run: python scripts/setup_tts.py
style-bert-vits2>=2.5.0
huggingface-hub>=0.20.0
